[project]
name = "agent-workflow-lab"
version = "0.1.0"
description = "Fixture-driven weekly practice lab for AI Agent service development"
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.115",
  "langchain>=1.0,<2.0",
  "langchain-text-splitters>=1.0,<2.0",
  "langgraph>=1.0,<2.0",
  "uvicorn>=0.30",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "httpx2>=2.13.0",
]
